import type { ScheduleListConponentProp } from "../../shared/types/ScheduleType"

const ScheduleList = ({ width, height, layout, range, title }: ScheduleListConponentProp) => {

    if (layout === 'hor')
        return (
            <div>
                
            </div>
        );
    else
        return (
            <div>

            </div>
        )
}

export default ScheduleList